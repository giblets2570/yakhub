div#sub-nav.navbar.navbar-default.navbar-static-top(ng-controller='navbarSubCtrl')
	div.container(style='color: white;')
		div.navbar-collapse.collapse(collapse='isCollapsed')
			ul.nav.navbar-nav.nav-text
				li
					if type == "dashboard"
						a.chosen(href='', ng-click='go("dashboard")') Dashboard
					else
						a(href='', ng-click='go("dashboard")') Dashboard
				li
					if type == "analytics"
						a.chosen(href='', ng-click='go("analytics")') Analytics
					else
						a(href='', ng-click='go("analytics")') Analytics
				li
					if type == "admin"
						a.chosen(href='', ng-click='go("schedule")') Admin
					else
						a(href='', ng-click='go("schedule")') Admin
			//- ul.nav.navbar-nav.navbar-right.nav-text
			//- 	li
			//- 		a(href='')
			//- 			p.campaign Campaign Settings
			//- 			span.glyphicon.glyphicon-cog