div#admin-nav.navbar.navbar-default.navbar-static-top(ng-controller='navbarSubCtrl')
	div.container(style='color: white;')
		div.navbar-collapse.collapse(collapse='isCollapsed')
			ul.nav.navbar-nav.nav-text
				li
					if admin == "schedule"
						a.chosen(href='', ng-click='go("schedule")') Schedule
					else
						a(href='', ng-click='go("schedule")') Schedule
				li
					if admin == "info"
						a.chosen(href='', ng-click='go("info")') Info
					else
						a(href='', ng-click='go("info")') Info
				li
					if admin == "script"
						a.chosen(href='', ng-click='go("script")') Script
					else
						a(href='', ng-click='go("script")') Script
				li
					if admin == "numbers"
						a.chosen(href='', ng-click='go("numbers")') Numbers
					else
						a(href='', ng-click='go("numbers")') Numbers
				li
					if admin == "agents"
						a.chosen(href='', ng-click='go("agents")') Agents
					else
						a(href='', ng-click='go("agents")') Agents